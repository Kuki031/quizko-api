doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    style.
      body {
        background-color: rgb(240, 240, 240);
        color: #3c3c3c;
        font-family: Arial, sans-serif;
        padding: 20px;
      }
      .container {
        background-color: #ffffff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin: 0 auto;
        max-width: 600px;
        padding: 20px;
      }
      .header {
        background-color: #e1bf57;
        border-radius: 8px 8px 0 0;
        color: #ffffff;
        padding: 20px;
        text-align: center;
      }
      .content {
        padding: 20px;
      }
      a {
        color: #e1bf57;
        text-decoration: none;
        list-style:none;
      }
      .button {
        background-color: #e1bf57;
        border: none;
        border-radius: 5px;
        color: #3c3c3c;
        display: inline-block;
        margin-top: 20px;
        padding: 10px 20px;
        text-align: center;
        text-decoration: none;
      }
      .footer {
        background-color: #3c3c3c;
        border-radius: 0 0 8px 8px;
        color: rgb(255, 255, 255);
        padding: 10px;
        text-align: center;
      }

  body
    .container
      .header
        h2=`${heading}`
      .content
        p= `${par_1}`
        p= `${par_2}`
        a.button(href=`${link}/${user_id}/${token}`)= `${anchor}`
      .footer
        p © 2024 Quizko edIT
